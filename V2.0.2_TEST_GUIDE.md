# Bulwark v2.0.2 测试指南

## 📦 版本信息
- **版本**: v2.0.2
- **编译时间**: 2026-07-28
- **修复重点**: 文件清理失败问题（VT 释放物签名绕过）

---

## 🎯 本次修复的问题

### 原问题
用户报告："释放的文件还是清理不干净，老是提示清理失败"

### 根本原因
1. **签名保护过严** - VT 沙箱报告的释放文件（`droppedFilePaths`）即使是恶意的，只要带签名就被跳过
2. **落地区太窄** - 很多恶意软件释放到用户根目录、C:\temp 等位置，不在原有的清理白名单中

### 修复内容
1. ✅ VT 确认的释放物现在会**绕过签名保护**（与哈希精确匹配同等待遇）
2. ✅ 扩展清理落地区：新增 `\users\`、`c:\temp\`、`c:\tmp\`、`\music\`、`\videos\`、`\pictures\`

---

## 🚀 快速测试步骤

### 步骤 1: 启动新版本
```powershell
# 停止旧版本（如果正在运行）
Stop-Process -Name "bulwark_service" -Force -ErrorAction SilentlyContinue
Stop-Process -Name "bulwark_ui" -Force -ErrorAction SilentlyContinue

# 启动新版本（管理员权限）
d:\新建文件夹 (3)\cpp\dist\bulwark_ui.exe
```

### 步骤 2: 双击恶意样本
- 找一个之前提示"清理失败"的样本
- 双击样本触发 VT 扫描
- 观察 UI 上的清理报告

### 步骤 3: 检查日志
```powershell
# 查看情报日志（应该看到规则生成）
Get-Content "C:\ProgramData\Bulwark\service.log" -Tail 50 | Select-String "情报|规则"

# 查看清理日志（应该看到"已隔离"而不是"跳过"）
Get-Content "C:\ProgramData\Bulwark\service.log" -Tail 50 | Select-String "清理|隔离|跳过"
```

### 步骤 4: 验证规则生成
```powershell
# 查看生成的规则（应该有 DNS 和注册表规则）
Get-Content "C:\ProgramData\Bulwark\rules.json" | ConvertFrom-Json | 
    Where-Object { $_.note -like "*情报-行为*" } | 
    Select-Object type, targetPattern, action, note
```

---

## ✅ 预期结果

### 日志输出示例
```
[INFO] 情报行为画像[VirusTotal]:释放文件 5、注册表 3、外联IP 2、域名 1;已注入主动拦截规则 11 条。
[WARN] 足迹清理:已隔离恶意释放文件 C:\Users\admin\AppData\Local\Temp\malware.exe
[WARN] 足迹清理:已隔离恶意释放文件 C:\Users\admin\Desktop\dropper.dll
[WARN] 足迹清理完成:隔离文件 5 个,移除自启动项 3 个。
```

### UI 清理报告
- **隔离文件列表** 应该包含所有 VT 报告的释放文件
- **跳过文件列表** 应该大幅减少（只剩系统目录的合法文件）
- **原因统计**:
  - ✅ "位于系统/安装目录,保护不动" - 正常，保护系统文件
  - ✅ "隔离失败,可能被占用" - 正常，运行中的文件会后续清理
  - ❌ "带可信数字签名,保护不动" - **不应再出现**（VT 释放物会绕过）

---

## 🔍 如果还是清理失败

### 情况 1: 文件被占用
**原因**: 恶意进程还在运行，文件被锁定  
**解决**: 检查进程是否被正确终止
```powershell
Get-Content "C:\ProgramData\Bulwark\service.log" | Select-String "结束进程"
```

### 情况 2: 文件在系统目录
**原因**: 位于 `\Windows\System32\` 等保护区  
**解决**: 这是正常的安全机制，不应强制删除系统目录文件

### 情况 3: 路径不匹配
**原因**: VT 沙箱路径与本机路径不一致  
**示例**: VT 报告 `C:\Users\sandbox\xxx.exe`，但本机用户名不同  
**解决**: 代码已自动翻译路径到所有本机用户目录

### 情况 4: 哈希定位失败
**原因**: `locateDroppedFilesByHash()` 未找到文件  
**解决**: 检查日志中是否有"哈希定位"相关输出

---

## 📊 对比测试

### v2.0.1（修复前）
```
跳过: C:\Users\admin\AppData\Local\Temp\signed_malware.exe
原因: 带可信数字签名,保护不动
```

### v2.0.2（修复后）
```
已隔离: C:\Users\admin\AppData\Local\Temp\signed_malware.exe
```

---

## 🛠️ 调试技巧

### 查看 VT 报告原始数据
```powershell
Get-Content "C:\ProgramData\Bulwark\service.log" | 
    Select-String "droppedFilePaths|registryKeysSet|contactedDomains"
```

### 检查签名绕过逻辑
```powershell
Get-Content "C:\ProgramData\Bulwark\service.log" | 
    Select-String "bypassSignatureGuard|VT 确认|哈希精确"
```

### 验证文件清理候选列表
```powershell
Get-Content "C:\ProgramData\Bulwark\service.log" | 
    Select-String "maliciousFiles|consider|vtDroppedLower"
```

---

## 📝 问题反馈

如果还是提示"清理失败"，请提供：
1. 完整的 `C:\ProgramData\Bulwark\service.log`（最近 200 行）
2. 样本的 SHA-256（如果可以分享）
3. UI 上显示的清理报告截图
4. 跳过文件的具体原因

---

## 🎓 技术原理

### 文件清理决策流程（v2.0.2）
```
1. 收集恶意文件列表
   ├─ 主体 actorPath
   ├─ 行为足迹 FileWrite/Delete
   ├─ VT 报告 droppedFilePaths（翻译到本机）
   └─ 哈希定位 locatedLocalPaths

2. 逐个文件检查
   ├─ 文件是否存在？
   ├─ 是否在保护区（System32/Program Files）？ → 跳过
   ├─ 是否在落地区（Temp/AppData/Users...）？ → 继续
   └─ 是否需要绕过签名保护？
       ├─ 主体签名异常？ → 绕过
       ├─ 哈希精确匹配？ → 绕过
       └─ VT 确认释放物？ → 绕过 ⭐ v2.0.2 新增

3. 签名检查
   ├─ bypass=true → 直接隔离
   └─ bypass=false → 检查签名，有签名则跳过

4. 隔离操作
   ├─ 成功 → 记录"已隔离"
   └─ 失败 → 记录"隔离失败,可能被占用"
```

### 关键代码位置
- 签名绕过逻辑: `cpp/service/src/ThreatRemediator.cpp:305-310`
- 落地区定义: `cpp/service/src/ThreatRemediator.cpp:116-125`
- 安全检查: `cpp/service/src/ThreatRemediator.cpp:225-235`
