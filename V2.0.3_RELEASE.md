# Bulwark v2.0.3 正式版本

## 🎉 版本信息
- **版本号**: v2.0.3
- **编译时间**: 2026-07-28
- **发布状态**: ✅ Ready for Testing
- **重点修复**: 系统工具误删保护 + 文件清理失败问题

---

## 🔥 本次更新亮点

### 1. ⭐ 新增：系统工具保护白名单
**问题**: 用户报告 Visual Studio 开发工具快捷方式（cmd.exe、PowerShell）被误删  
**修复**: 添加 21 个系统工具的**绝对保护**白名单

保护列表:
- Shell: `cmd.exe`, `powershell.exe`, `pwsh.exe`
- 工具: `taskmgr.exe`, `regedit.exe`, `notepad.exe`, `explorer.exe`
- 系统: `svchost.exe`, `lsass.exe`, `winlogon.exe`, `csrss.exe`
- 完整列表见 `SYSTEM_TOOL_PROTECTION.md`

### 2. ✅ 修复：文件清理失败问题
**问题**: 释放的文件还是清理不干净，老是提示清理失败  
**原因**: 带签名的恶意释放物被跳过  
**修复**: VT 沙箱确认的释放文件现在会绕过签名保护

### 3. 📈 改进：扩展清理落地区
新增 6 个常见恶意软件落地区:
- `\users\` (用户根目录)
- `c:\temp\`, `c:\tmp\` (系统临时目录)
- `\music\`, `\videos\`, `\pictures\` (媒体文件夹)

---

## 📊 v2.0.1 → v2.0.3 对比

| 场景 | v2.0.1 | v2.0.3 |
|------|--------|--------|
| **带签名恶意释放物** | ❌ 跳过 "带可信数字签名" | ✅ 隔离（VT 确认） |
| **用户根目录恶意文件** | ❌ 跳过 "不在落地区" | ✅ 隔离（新增落地区） |
| **系统工具 (cmd.exe)** | ⚠️ 可能被误删 | ✅ 绝对保护（白名单） |
| **VS 开发工具快捷方式** | ⚠️ 可能被误删 | ✅ 保护（目标为系统工具） |

---

## 🚀 快速开始

### 步骤 1: 停止旧版本
```powershell
Stop-Process -Name "bulwark_service" -Force -ErrorAction SilentlyContinue
Stop-Process -Name "bulwark_ui" -Force -ErrorAction SilentlyContinue
Start-Sleep 2
```

### 步骤 2: 启动 v2.0.3
```powershell
# 管理员权限启动
d:\新建文件夹 (3)\cpp\dist\bulwark_ui.exe
```

### 步骤 3: 测试清理功能
1. 找一个之前"清理失败"的恶意样本
2. 双击样本触发 VT 扫描
3. 观察清理报告（应该看到更多文件被隔离）

### 步骤 4: 验证系统工具保护
```powershell
# 查看保护日志
Get-Content "C:\ProgramData\Bulwark\service.log" -Tail 50 | 
    Select-String "系统关键工具"

# 如果看到输出，说明保护机制生效:
# [INFO] 威胁清理:跳过 cmd.exe, 原因: 系统关键工具,绝对保护
```

---

## ✅ 预期结果

### 清理报告改善
**v2.0.1 (旧版)**:
```
隔离文件: 2 个
跳过文件: 8 个
  - 带可信数字签名,保护不动: 5 个
  - 不在用户可写落地区: 3 个
```

**v2.0.3 (新版)**:
```
隔离文件: 7 个  ⬆️ 增加 5 个
跳过文件: 3 个  ⬇️ 减少 5 个
  - 系统关键工具,绝对保护: 1 个
  - 位于系统/安装目录: 2 个
```

### 日志输出示例
```
[INFO] 情报行为画像[VirusTotal]:释放文件 5、注册表 3、外联IP 2、域名 1;已注入主动拦截规则 11 条。
[WARN] 足迹清理:已隔离恶意释放文件 C:\Users\admin\Desktop\malware.exe
[WARN] 足迹清理:已隔离恶意释放文件 C:\Users\admin\Music\dropper.dll
[INFO] 威胁清理:跳过 C:\Windows\System32\cmd.exe, 原因: 系统关键工具,绝对保护
[WARN] 足迹清理完成:隔离文件 7 个,移除自启动项 3 个。
```

---

## 🔍 清理决策流程（完整版）

```
                    文件清理候选列表
                           ↓
    ┌──────────────────────────────────────┐
    │ 1. 系统工具白名单检查 ⭐ v2.0.3 新增 │
    └──────────────────────────────────────┘
                  ↓ 否
    ┌──────────────────────────────────────┐
    │ 2. 系统/安装目录检查                  │
    │    (System32/Program Files)          │
    └──────────────────────────────────────┘
                  ↓ 否
    ┌──────────────────────────────────────┐
    │ 3. 用户可写落地区检查                 │
    │    (Temp/AppData/Downloads + 6个新区) │
    └──────────────────────────────────────┘
                  ↓ 是
    ┌──────────────────────────────────────┐
    │ 4. 签名保护绕过检查                   │
    │    - 主体签名异常                     │
    │    - 哈希精确匹配                     │
    │    - VT 确认释放物 ⭐ v2.0.2 新增    │
    └──────────────────────────────────────┘
                  ↓
    ┌──────────────────────────────────────┐
    │ 5. 数字签名检查                       │
    └──────────────────────────────────────┘
                  ↓
             ✅ 隔离文件
```

---

## 🛡️ 安全保障

### 多层保护机制
1. **系统工具白名单** (最高优先级) - 绝对不删 cmd/powershell 等
2. **系统目录保护** - 不碰 System32/Program Files
3. **落地区限制** - 只清理用户可写区域
4. **签名验证** - 保护合法签名文件（除非 VT 确认恶意）

### 零误删策略
- 宁可漏过恶意副本（如 `C:\Temp\cmd.exe`）
- 也不能误删真正的系统工具
- 所有系统工具按文件名保护，不检查路径

---

## 📝 完整功能清单

### 实时主动防护 (v2.0.1+)
- ✅ 释放文件哈希拦截 (ProcessCreate → Block)
- ✅ C2 外联 IP 拦截 (NetworkConnect → Block)
- ✅ C2 域名 DNS 拦截 (DnsQuery → Block)
- ✅ 释放文件名监控 (FileWrite → Ask)
- ✅ 注册表持久化拦截 (RegistryWrite → Block)

### 威胁清理 (v2.0.3 增强)
- ✅ 文件隔离 (主体 + 释放物 + 哈希定位)
- ✅ 注册表清理 (Run/IFEO/Services/Winlogon/AppInit/Task/Startup)
- ✅ 进程终止 (进程树 + 内核补刀)
- ✅ 持久化防重建 (内核硬拦)
- ⭐ 系统工具保护 (v2.0.3 新增)

### 情报集成
- ✅ VirusTotal 行为报告
- ✅ 威胁情报自动规则生成
- ✅ 本地哈希匹配

---

## 🐛 已知限制

### 1. 系统工具副本保护
**现象**: `C:\Temp\cmd.exe` (恶意副本) 也会被保护  
**原因**: 只检查文件名，不检查路径  
**影响**: 低 - 恶意副本通常不会用真实的系统工具名  
**改进**: 未来版本可能增加路径 + 签名双重验证

### 2. 文件被占用清理失败
**现象**: 运行中的恶意进程文件隔离失败  
**原因**: 文件被独占锁定  
**解决**: QuarantineManager 内部使用内核"忽略共享检查"读取 + POSIX 强制删除  
**影响**: 低 - 已尽力处理，实在被占用会记录"隔离失败,可能被占用"

### 3. VT 路径翻译可能失配
**现象**: VT 沙箱路径与本机路径不同  
**解决**: 代码已自动翻译 `\Users\<name>\` 到所有本机用户目录  
**影响**: 低 - 大部分情况能正确匹配

---

## 📚 相关文档

- `SYSTEM_TOOL_PROTECTION.md` - 系统工具保护详细说明
- `V2.0.2_TEST_GUIDE.md` - v2.0.2 测试指南（签名绕过）
- `PROACTIVE_DEFENSE.md` - 主动防护技术文档
- `REAL_TIME_PROTECTION_STATUS.md` - 实时防护状态分析
- `CHANGELOG.md` - 完整更新记录

---

## 🔧 故障排查

### 问题 1: 还是提示"清理失败"
**检查步骤**:
```powershell
# 查看跳过原因
Get-Content "C:\ProgramData\Bulwark\service.log" -Tail 100 | 
    Select-String "跳过|清理"
```

**可能原因**:
- ✅ "系统关键工具" - 正常，保护机制
- ✅ "位于系统/安装目录" - 正常，不应删除
- ⚠️ "隔离失败,可能被占用" - 文件正在运行，需要先终止进程
- ❌ "带可信数字签名" - **不应再出现**（VT 释放物会绕过）

### 问题 2: VS 开发工具快捷方式被删
**v2.0.3 应该已修复**，如果还有问题:
```powershell
# 检查系统工具保护日志
Get-Content "C:\ProgramData\Bulwark\service.log" | 
    Select-String "系统关键工具"
```

如果没有看到保护日志，说明白名单未生效，请提供完整日志。

### 问题 3: 合法文件被误删
**报告方式**:
1. 提供文件路径
2. 提供文件 SHA-256
3. 提供 `service.log` 中的清理相关日志
4. 说明文件的合法用途

---

## 🎯 测试清单

- [ ] 启动 v2.0.3 成功
- [ ] VT 扫描恶意样本触发清理
- [ ] 查看清理报告（隔离文件数增加）
- [ ] 验证系统工具未被删除（cmd/powershell 正常使用）
- [ ] 检查日志输出（无异常错误）
- [ ] VS 开发工具快捷方式正常使用
- [ ] 主动防护规则生成（DNS + 注册表）

---

## 💬 反馈

如有任何问题，请提供:
1. 完整的 `C:\ProgramData\Bulwark\service.log` (最近 300 行)
2. 清理报告截图（UI 上显示的跳过原因）
3. 被误删或未清理的文件路径
4. 样本 SHA-256（如果可以分享）

---

**祝测试顺利！🚀**
